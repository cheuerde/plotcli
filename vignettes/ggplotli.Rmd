---
title: "Introduction to ggplotcli"
author: "Claas Heuer"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Introduction to ggplotcli}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---
  
```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

## Using `ggplotcli` with `ggplot2`

The `ggplotcli()` function converts any ggplot2 plot to a terminal-based visualization.

```{r}
library(plotcli)
library(ggplot2)

# Create a ggplot with points and smooth line
mtcars_ggplot <- ggplot(mtcars, aes(x = mpg, y = wt)) +  
  geom_point() +  
  geom_smooth(method = "lm", color = "red") +
  labs(title = "Mtcars Dataset with Regression Line",  
       x = "Miles per Gallon",  
       y = "Weight")  

# Render in terminal
suppressMessages(ggplotcli(mtcars_ggplot, width = 60, height = 15))
```

## Canvas Types

You can choose different rendering styles:

```{r}
p <- ggplot(mtcars, aes(x = wt, y = mpg)) + 
  geom_point(color = "blue") +
  labs(title = "Braille Canvas (default)")

# High resolution with Braille
ggplotcli(p, width = 50, height = 10, canvas_type = "braille")

# ASCII for maximum compatibility
ggplotcli(p + labs(title = "ASCII Canvas"), width = 50, height = 10, canvas_type = "ascii")
```

## Styling Options

```{r}
p <- ggplot(mtcars, aes(x = hp, y = qsec)) +
  geom_point(color = "green") +
  labs(title = "With Border and Grid")

# Add border and grid
ggplotcli(p, width = 50, height = 10, border = TRUE, grid = "major")
```
